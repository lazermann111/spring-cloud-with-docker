eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka
      fetch-registry: true
      registerWithEureka: true
spring:
  application:
    name: school-ui
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
      failFast: true

